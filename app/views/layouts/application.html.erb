<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <title>HabitSecondApp</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="text-center">
    <div class="cover-container d-flex h-100 p-3 mx-auto flex-column">
      <header class="masthead mb-auto">
        <div class="inner">
          <h3 class="masthead-brand">癖直し煽りマシーン</h3>
          <nav class="nav nav-masthead justify-content-center">
            <% if logged_in? %>
            <span class="nav-link active"><%= link_to 'ログアウト', logout_path %></span>
            <% else %>
            <span class="nav-link active"><%= link_to 'Twitterでログイン', '/auth/twitter' %></span>
            <% end %>
          </nav>
          <% if flash[:notice] %>
          <div class="alert alert-success">
            <%= flash[:notice] %>
          </div>
          <% end %>
        </div>
      </header>
      <main role="main" class="inner cover">
        <h1 class="cover-heading">直したい癖を書いてください</h1>
        <p class="lead">例：タバコをやめたい。晩酌をやめたい。女遊びをやめたい。なんでも....</p>
        <div class="container" style="margin-top:50px;">
          <%= yield %>
        </div>
      </main>
      <footer class="mastfoot mt-auto" >
        <div class="inner" style="margin-top:80px;">
          <p>Cover template for <a href="https://getbootstrap.com/">Bootstrap</a>, by <a href="https://twitter.com/mdo">@mdo</a>.</p>
        </div>
      </footer>
    </div>
  </body>
  </html>
